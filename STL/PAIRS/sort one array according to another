void sortArray(int a[], char b[], int n){
    
    // code here
    pair<int,char> p[n];
    
    for(int i=0;i<n;i++)
    {
        p[i]={a[i],b[i]};
    }
    sort(p,p+n);
    for(int i=0;i<n;i++)
    {
        cout<<p[i].second<<" ";
    }
    
    
}
