vector<int> displayList(Node *head)
{
    
    vector<int>k;
    while(head->next!=NULL)
    {
        k.push_back(head->data);
        head=head->next;
    }
    k.push_back(head->data);
    return k;
    
}
