	
   int fibo(int n)
   {
       int dp[n+1]={-1};
       
       dp[0]=0,dp[1]=1;
       for(int i=2;i<=n;i++)
       {
           dp[i]=dp[i-1]+dp[i-2];
       }
       return dp[n];
   }
   	int evaluate_exp(int n){
   	    // Code here
   	    return fibo(n-1)*fibo(n+1)-fibo(n)*fibo(n);
   	} 
